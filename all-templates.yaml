apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: example-templates
  description: A collection of all Backstage example Templates
spec:
  targets:
    - ./template/azure-repository-template.yaml
